import type { Metadata } from "next";
export const metadata:Metadata={title:"資料結構管理後台",description:"資料結構教材上傳、自動拆解與檢索管理。"};
export default function Layout({children}:{children:React.ReactNode}){return children}
