"use client";
import { useEffect, useState } from "react";
type Row={id:number;topic:string;aiReply:string;studentQuestion:string;verificationResult:string;verificationSources:{label:string;url?:string}[];status:string;teacherReply:string;teacherRepliedAt?:string|null;studentReadAt?:string|null;createdAt:string};
export default function PengliNotes(){const[rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true);async function load(){const response=await fetch("/api/teachers/pengli/questions",{cache:"no-store"});if(response.ok){const data=await response.json();setRows(data.rows||[])}setLoading(false)}useEffect(()=>{void load()},[]);async function read(row:Row){if(row.status==="answered"&&!row.studentReadAt){await fetch("/api/teachers/pengli/questions",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:row.id,action:"read"})});void load()}}
return <main className="pengli-notes"><header><div><small>彭狸老師專區</small><h1>我的筆記</h1><p>保存 AI 查證、我的疑問與老師本人回覆。</p></div><a href="/teachers/pengli/coach">← 回去繼續對話</a></header>{loading?<p>正在載入筆記…</p>:rows.length===0?<section className="empty">尚未有疑問筆記。你可以在 AI 教練的回覆下按「我有疑問」。</section>:<section className="grid">{rows.map(row=><article key={row.id} className={row.status==="answered"&&!row.studentReadAt?"unread":""} onClick={()=>void read(row)}><div className="status">{row.status==="answered"&&!row.studentReadAt?"✉ 老師新回覆":row.status==="pending_teacher"?"等待老師回覆":"AI 已查證"}</div><h2>{row.topic}</h2><b>原 AI 回覆</b><p>{row.aiReply}</p><b>我的疑問</b><p>{row.studentQuestion}</p><b>AI 查證</b><p>{row.verificationResult}</p>{row.teacherReply&&<div className="teacher"><b>彭狸老師回覆</b><p>{row.teacherReply}</p></div>}</article>)}</section>}</main>}
