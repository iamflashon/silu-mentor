import { getAccountingProductSettings } from "../../../../../lib/accounting-product-settings";
import { getDb } from "../../../../../db";
export async function GET(){const db=await getDb();const product=await getAccountingProductSettings(db);if(!product.coverStorageKey)return new Response("not found",{status:404});const{env}=await import("cloudflare:workers"),object=await env.BUCKET.get(product.coverStorageKey);if(!object)return new Response("not found",{status:404});const responseHeaders=new Headers();object.writeHttpMetadata(responseHeaders);responseHeaders.set("etag",object.httpEtag);responseHeaders.set("cache-control","public, max-age=300");return new Response(object.body,{headers:responseHeaders})}
