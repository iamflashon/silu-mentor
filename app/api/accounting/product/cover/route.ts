import { getAccountingProductSettings } from "../../../../../lib/accounting-product-settings";
import { requireMember } from "../../../../../lib/member-auth";
export async function GET(request:Request){const auth=await requireMember(request);if("error"in auth)return auth.error;const product=await getAccountingProductSettings(auth.db);if(!product.coverStorageKey)return new Response("not found",{status:404});const{env}=await import("cloudflare:workers"),object=await env.BUCKET.get(product.coverStorageKey);if(!object)return new Response("not found",{status:404});const responseHeaders=new Headers();object.writeHttpMetadata(responseHeaders);responseHeaders.set("etag",object.httpEtag);responseHeaders.set("cache-control","private, max-age=300");return new Response(object.body,{headers:responseHeaders})}
